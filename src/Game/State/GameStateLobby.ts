import GameStateBase from "./GameStateBase";

export default class GameStateLobby extends GameStateBase{
    constructor(battle) {
        super(battle);
    }

    public enter(): void {
        super.enter();
    }

    public leave(): void {
        super.leave();
    }

    public reset(): void {
        super.reset();
    }

    public update(): void {
        super.update();
    }
}